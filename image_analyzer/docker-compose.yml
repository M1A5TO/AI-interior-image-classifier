version: "3.9"

services:
  image-analyzer:
    build: .
    container_name: image-analyzer
    env_file:
      - .env
    restart: unless-stopped
    volumes:
      - ./app/lora_models:/app/app/lora_models
