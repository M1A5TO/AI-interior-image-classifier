version: "3.9"

services:
  image-analyzer:
    build: .
    # container_name removed to allow scaling multiple replicas
    env_file:
      - .env
    restart: unless-stopped
    volumes:
      - ./app/lora_models:/app/app/lora_models
